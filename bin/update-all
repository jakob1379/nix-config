#!/usr/bin/env bash
set -euo pipefail

sudo -v
nix-channel --update &
sudo nix-channel --update &
wait

hsu "$@"

uv tool upgrade --all &
rm -rf "$HOME/.cache/opencode"
opencode models --refresh &

wait

sudo -v
nh os switch "$HOME/.config/home-manager/"
